# ⚠️ DATOS PERSONALES REALES — nunca subir al repo público
docs/insumos/fotmato LISTADO DE ASISTENCIA.xlsx
docs/insumos/*ASISTENCIA*.xlsx
docs/insumos/*asistencia*.xlsx

# Dependencias y builds (futuro)
node_modules/
dist/
build/
.env
.env.*
# amplify_outputs.json — SE COMMITEA: solo contiene IDs públicos del cliente (van en el bundle JS igualmente)
.amplify/

# Sistema
.DS_Store
Thumbs.db

# 🔐 Credenciales AWS — NUNCA subir
usuario_administrador_aws_guia/
*accessKeys*.csv
*_credentials*.csv
