export default function App() {
  return (
    <main className="wrap" style={{ padding: '4rem 2rem', textAlign: 'center' }}>
      <h1 style={{ fontFamily: 'var(--font-display)' }}>GuIA ∞</h1>
      <p style={{ color: 'var(--ink-2)' }}>
        Plataforma de cursos adaptativos con IA — build en progreso (E1).
      </p>
    </main>
  );
}
